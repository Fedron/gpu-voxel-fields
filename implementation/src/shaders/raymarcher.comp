#version 430

layout(local_size_x = 16, local_size_y = 16) in;

uniform layout(rgba8, binding = 0) writeonly image2D out_image;

void main() {
    ivec2 pixel_coords = ivec2(gl_GlobalInvocationID.xy);
    imageStore(out_image, pixel_coords, vec4(255.0, 0.0, 0.0, 255.0));
}